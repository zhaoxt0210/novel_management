spring:
  application:
    name: novel-management-system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://82.156.240.2:3306/novel_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&connectTimeout=60000&socketTimeout=60000&cachePrepStmts=true&useServerPrepStmts=true&prepStmtCacheSize=256&prepStmtCacheSqlLimit=2048
    username: novel_root
    password: novel_123456
    hikari:
      connection-timeout: 60000
      validation-timeout: 10000
      max-lifetime: 1800000
      idle-timeout: 600000
      minimum-idle: 10
      maximum-pool-size: 30
      connection-test-query: SELECT 1
      leak-detection-threshold: 60000
      max-idle-time: 600000
      pool-name: NovelManagementHikariCP
      register-mbeans: true
      allow-pool-suspension: false
      login-timeout: 60
      initialization-fail-timeout: 0

  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 2MB
      file-size-threshold: 0

  transaction:
    default-timeout: 120

server:
  port: 8080
  tomcat:
    connection-timeout: 120000
    max-http-form-post-size: 2MB

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.novel.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    default-statement-timeout: 120
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: novelSecretKey12345678901234567890123456789012
  expiration: 86400000

file:
  upload:
    path: ./uploads/
  access:
    url: /uploads/

logging:
  level:
    com.novel: debug